import './globals.css'

export const metadata = {
  title: 'golden88',
  description: 'Your Golden Gateway to Big Wins',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
